import AfterMessage from "components/general/AfterMessage";

const AfterSignIn = () => {
    return (
      <AfterMessage message="Signed-in successfully."></AfterMessage>
    )
};

export default AfterSignIn;